function getFetchObject(){
    var fetchObject = {
        load:function(selector, url, method, data, complete){
            //TODO
        },
        request:function(url, method, data, complete){
            //TODO
        }
    };
    return fetchObject;
}

export default getFetchObject;

